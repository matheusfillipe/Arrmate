repos:
  - repo: local
    hooks:
      - id: arrmate-precommit
        name: make precommit (ruff format + imports + lint)
        entry: make precommit
        language: system
        pass_filenames: false
        types_or: [python]
